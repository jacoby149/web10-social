12.13.22
make the chat deletor effect the data model.
    - selecting chat message IDs effects a selectedMessages list of ids state.
    - deletion button triggers an interface function that deletes the messages.
make the postmaker/editor effect the data model.
    - uploading and ex ing media effects the draftpost.
    - textbox input effects the draftpost
    - confirmation and cancellation buttons trigger the changes.
make the search bar work for messages, contacts, feed, and wall
MEET WITH JOHN

web10 integrations.
add default data for non existing users.
hook up the web10 login to the chat application.
hook up the web10 services to :
    - contacts [ make the add contacts effect the data model ]
    - messages
    - social-identity
    - social-bulletin
    - social-posts
add logo / PWA functions

step 2 : hook up simple snake game
    - read bio where type = snake score
    - empty? insert
    - update bio where type = snake_score set hi score.

step 3 : fix Sam's pixel art app
    - impose brick size limit
    - use easy to render brick colors
    - make a texture pallete
    - separate the viewing and editing properly
    - make multiple sculptures in the data
    - generate iframe in the web10 social-bulletin